Upload these batches to the `preview` GitHub repo in this order.

Important:
- Open each batch folder and drag the contents of that folder into GitHub.
- Do not drag the outer batch folder itself, or GitHub will create the wrong top-level folder name.
- Upload into the repo root unless GitHub already shows you are inside the matching folder path.

Recommended order:

1. `1-root-and-shared`
   This includes:
   - `index.html`
   - `work.html`
   - `projects.html`
   - `art.html`
   - `stuff.html`
   - `styles.css`
   - `projects.js`
   - `stuff.js`
   - `assets/fonts`
   - `assets/images`
   - `assets/stuff`

2. `2-projects-images-a`

3. `3-projects-images-b`

4. `4-projects-video-photos`

5. `5-projects-video-reel`

6. `6-projects-video-odessia`

7. `7-art-a`

8. `8-art-b`

9. `9-art-c`

10. `10-art-d`

After each upload:
- add a short commit message
- wait for GitHub to finish
- then start the next batch

After all uploads are done:
- go to `Settings > Pages`
- confirm it is publishing from `main` and `/(root)`
- your preview link should be `https://annieswanson.github.io/preview/`
